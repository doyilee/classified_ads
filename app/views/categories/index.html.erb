<h1> Classified Ads</h1>

<h3> Vehicles </h3>
<ul>
	<% @vehicle.items.each do |item| %>
		<li> <%= link_to("#{item.name}", item_path(item.id))%></li>
	<%end%>
</ul>


<h3> Housing </h3>
<ul>
	<% @housing.items.each do |item| %>
		<li> <%= link_to("#{item.name}", item_path(item.id))%></li>
	<%end%>
</ul>

<h3> Jobs </h3>
<ul>
	<% @jobs.items.each do |item| %>
		<li> <%= link_to("#{item.name}", item_path(item.id))%></li>
	<%end%>
</ul>

<h3> General </h3>
<ul>
	<% @general.items.each do |item| %>
		<li> <%= link_to("#{item.name}", item_path(item.id))%></li>
	<%end%>
</ul>
	<h3> Categories </h3>
		<% @categories.each do |category| %>
				<%= link_to("#{category.name}", category_path(category.id)) %>
		<%end%>
</div>
